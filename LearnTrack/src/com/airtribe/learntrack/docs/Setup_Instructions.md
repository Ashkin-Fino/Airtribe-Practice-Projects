JDK Vesion Used: 25.0.1

HelloWorld Rrogram Run:

    Screenshot Path: src/com/airtribe/learntrack/docs/Helloworld Program Screenshot.png
    Explanation:
        Line no 1, defines the package in which the class belongs to.
        Line no 3, defines the class 'Main'.
        Line no 4, defines the main method. This is the starting point of any java program. The main method can take String[] as input. This is useful while running the program from command line, where we can give inputs such as '--param1', '--param2'.
        Line no 5, prints the string 'Hello, World!' in the console.